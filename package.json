{
  "name": "openclaw-agent-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "pack": "node scripts/build-plugin.js",
    "deploy": "npm run pack && bash scripts/install-plugin.sh"
  }
}
